# Canonical HTTP(S) origin used by metadata, structured data, robots, and the sitemap.
SITE_URL=https://opportunities2027.simonesiega.com

# Read the canonical pipeline database without modifying it.
OPPORTUNITIES_DATABASE_PATH=../data/opportunities.db

# Serve sanitized CSV and JSON projections generated by the Python pipeline.
OPPORTUNITIES_PUBLIC_EXPORT_DIR=../data/exports
